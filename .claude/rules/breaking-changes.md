# Breaking Changes

When making breaking changes to CLI flags, environment variables, headers, or API fields:

- Document in `docs/release-notes/` for the next release
- Include migration steps (what to change, exact commands)
