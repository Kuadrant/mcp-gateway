apiVersion: v1
kind: Service
metadata:
  name: a2a-test-server
  labels:
    app: a2a-test-server
spec:
  ports:
  - name: http
    port: 9090
    targetPort: 9090
  selector:
    app: a2a-test-server
  type: ClusterIP
