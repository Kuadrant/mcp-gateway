apiVersion: v1
kind: Service
metadata:
  name: nemo-guardrails-custom
  labels:
    app: nemo-guardrails-custom
spec:
  selector:
    app: nemo-guardrails-custom
  ports:
    - name: http
      port: 8000
      targetPort: 8000
