apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# optional: only the [Full,NemoGuardrails] e2e specs use these, so they're
# kept out of the default test-servers set. deploy via
# `make deploy-nemo-guardrails-test-servers`.
namespace: mcp-test

resources:
  - llm-inference-sim-deployment.yaml
  - llm-inference-sim-service.yaml
  - nemo-guardrails-configmap.yaml
  - nemo-guardrails-deployment.yaml
  - nemo-guardrails-service.yaml
