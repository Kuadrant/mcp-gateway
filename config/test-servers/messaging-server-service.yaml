apiVersion: v1
kind: Service
metadata:
  name: mcp-messaging-server
  labels:
    app: mcp-messaging-server
spec:
  ports:
  - name: http
    port: 9090
    targetPort: 9090
  selector:
    app: mcp-messaging-server
  type: ClusterIP
