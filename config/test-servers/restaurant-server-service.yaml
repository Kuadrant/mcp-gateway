apiVersion: v1
kind: Service
metadata:
  name: mcp-restaurant-server
  labels:
    app: mcp-restaurant-server
spec:
  ports:
  - name: http
    port: 9090
    targetPort: 9090
  selector:
    app: mcp-restaurant-server
  type: ClusterIP
